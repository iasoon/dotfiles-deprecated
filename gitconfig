[user]
	email = ilion.beyst@gmail.com
	name = Ilion Beyst

[mergetool "fugitive"]
	cmd = vim -f -c \"Gdiff\" \"$MERGED\"

[merge]
	tool = fugitive

[push]
	default = simple

[alias]
	co = checkout
    pushit = !git push -u origin `git symbolic-ref --short HEAD`
